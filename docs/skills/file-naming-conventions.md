# File Naming Conventions

> Conventions for naming files within the project.

- Always use `*.yaml` instead of `*.yml` as YAML file extension
- Always use `*.ts` instead of `*.js` for TypeScript files
