---
name: code-files
description: File naming conventions for this project. Trigger when creating or renaming files, e.g. choosing between .yaml and .yml extensions.
---

# Files Naming Conventions

* Always use `*.yaml` instead of `*.yml` as YAML file extension
